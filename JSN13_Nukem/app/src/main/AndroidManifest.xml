<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobile.jsn.jsn13_nukem">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".EventoSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main activity -->
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NovoUsuario"
            android:label="@string/title_activity_novo_usuario"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PrincipalActivity"
            android:label="@string/title_activity_principal"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name="com.mobile.jsn.mapa.MapaActivity"
            android:label="@string/title_activity_mapa" />
        <activity
            android:name=".PerfilActivity"
            android:label="@string/title_activity_perfil"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CorridaActivity"
            android:label="@string/title_activity_corrida"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".EntregaActivity"
            android:label="@string/title_activity_entrega"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ProdutoActivity"
            android:label="@string/title_activity_produto"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".HospedagemActivity"
            android:label="@string/title_activity_hospedagem"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".RedeSocialActivity"
            android:label="@string/title_activity_rede_social"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SuporteActivity"
            android:label="@string/title_activity_suporte"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CursoActivity"
            android:label="@string/title_activity_curso"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".PagamentoActivity"
            android:label="@string/title_activity_pagamento"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".AtividadeActivity"
            android:label="@string/title_activity_atividade"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".TutorialActivity"
            android:label="@string/title_activity_tutorial" />
        <activity
            android:name=".AnuncioActivity"
            android:label="@string/title_activity_anuncio"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ComentarioActivity"
            android:label="@string/title_activity_comentario"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".NovaContaActivity"
            android:label="@string/title_activity_nova_conata" />
        <activity
            android:name=".TesteFullscreenActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_teste_fullscreen"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".BuscaActivity"
            android:label="@string/title_activity_busca"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>